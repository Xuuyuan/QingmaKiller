## 青马易战自动答题工具 QingmaKiller v1.2.0

**本工具仅供学习交流使用。**

### ⚠️ 强制升级提示

- 自本版本起，在线搜题功能已完全内置，**不再附带、也不再需要 `tikuAdapter.exe`**。旧版本压缩包中的主程序已无法独立完成搜题，请尽快升级。

### 更新内容

- **告别外部搜题程序**：网络题库搜题逻辑直接内置到主程序中，发行包体积从约 28MB 缩减到约 13MB，解压即用，不再有搜题服务的启动等待与进程残留问题。
- **多题库并发搜题**：内置 4 个免费题库源（不挂科、icodef、万能、题库海），每次搜题并发查询并智能投票选出最可能的答案，单个题库故障不影响其余题库。
- **支持付费题库（默认关闭）**：如需更高的搜题命中率，可在程序同目录创建 `banks.json` 配置文件，启用言溪、爱点、柠檬等付费题库或填写免费题库的付费 token，具体格式见 README。不创建该文件则只使用免费题库，行为与既往版本一致。
- **搜题日志更直观**：每题命中时会显示来自哪个题库源，方便判断付费题库是否物有所值。

### 使用方法

1. 下载下方的 `QingmaKiller_v1.2.0.zip` 压缩包。
2. 解压到任意文件夹, 双击运行 `QingmaKiller.exe`, 按提示输入参数即可(网络题库已内置, 无需任何外部服务或配置)。

### 注意事项

- 程序未做代码签名, 首次运行如遇 Windows SmartScreen 提示, 点击【更多信息】→【仍要运行】; 若被杀毒软件误报, 请添加信任后使用。
- 本地题库 `tiku.json` 位于程序同目录, 答题过程中会自动补充, 也可手动编辑。
- 网络题库源为第三方公共接口, 可能随时间失效; 单个题库失效只会降低命中率, 不影响程序运行。
- 程序运行日志见同目录 `qingmakiller.log`, 遇到异常时可通过该文件定位问题。
