## 青马易战自动答题工具 QingmaKiller v1.1.1

**本工具仅供学习交流使用。**

### 更新内容

- 修复达到目标答对数且满足保底正确率后仍继续答题的问题。
- 修复保底正确率输入 100% 后无法完成上限配置的问题，现在会提示重新输入。
- 课程列表、取题、提交答案和 URL 认证的请求超时统一为 5 秒，避免网络异常时长时间等待。
- 增强在线搜题异常处理，答案格式异常时自动跳过本题。
- 优化题库文件定位，从其他工作目录启动时仍读取和保存程序同目录的题库。
- 精简答题决策、统计和会话复用实现，配置提示与实际默认值保持一致。

### 使用方法

1. 下载下方的 `QingmaKiller_v1.1.1.zip` 压缩包。
2. 解压到任意文件夹, 双击运行 `QingmaKiller.exe`, 按提示输入参数即可(搜题服务 tikuAdapter 会随主程序自动启动/关闭, 无需手动运行)。

### 注意事项

- 程序未做代码签名, 首次运行如遇 Windows SmartScreen 提示, 点击【更多信息】→【仍要运行】; 若被杀毒软件误报, 请添加信任后使用。
- 本地题库 `tiku.json` 位于程序同目录, 答题过程中会自动补充, 也可手动编辑。
- 程序运行日志见同目录 `qingmakiller.log`, 遇到异常时可通过该文件定位问题。
